import 'package:flutter/material.dart';

class HealthRecords extends StatelessWidget {
  const HealthRecords({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Health Rec");
  }
}
